{
    "build": {
        "dockerfile": "../Dockerfile",
        "context": "..",
        "args": {
            "NEWUSER": "${localEnv:USERNAME}"
        }
    },
    "extensions": [
        "ms-python.python",
        "jcbuisson.vue",
        "esbenp.prettier-vscode"
    ],
    "remoteUser": "${localEnv:USERNAME}"
}